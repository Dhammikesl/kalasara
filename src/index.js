import React from "react";
import ReactDOM from "react-dom";

import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); 
// saying this app component going to be rendered inside of the dev with ID root 
